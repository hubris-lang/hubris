language: rust
rust:
  - nightly
sudo: false
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-precise
    - llvm-toolchain-precise-3.6
    packages:
    - llvm-3.6
    - llvm-3.6-dev
    - libedit-dev
env:
  - LLVM_CONFIG="llvm-config-3.6"
